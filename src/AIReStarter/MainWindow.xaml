<Window x:Class="AIReStarter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI reStarter v2 (C# PoC)"
        Height="420"
        Width="620"
        ResizeMode="CanMinimize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="AI reStarter v2 - C#/.NET 10 PoC"
                   FontSize="18"
                   FontWeight="SemiBold" />

        <StackPanel Grid.Row="1" Margin="0,8,0,12">
            <TextBlock Text="ホットキー: Ctrl+Alt+P で一時停止/再開、Ctrl+Alt+Q で安全終了" />
            <TextBlock Text="設定: profiles.toml（存在しない場合は profiles.example.toml を使用）" />
        </StackPanel>

        <Border Grid.Row="2"
                BorderBrush="#DDD"
                BorderThickness="1"
                Padding="8"
                CornerRadius="6">
            <StackPanel>
                <TextBlock Text="検出対象モニター" FontWeight="SemiBold" Margin="0,0,0,6" />
                <ItemsControl ItemsSource="{Binding Monitors}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0"
                    VerticalAlignment="Center">
            <TextBlock x:Name="StatusText"
                       VerticalAlignment="Center"
                       FontWeight="SemiBold"
                       Margin="0,0,8,0" />
            <Button Content="監視開始/再開" Width="120" Margin="0,0,8,0" Click="OnStartClicked" />
            <Button Content="一時停止" Width="100" Click="OnPauseClicked" />
        </StackPanel>
    </Grid>
</Window>
